<!DOCTYPE html>
<html>
<head>
</head>
<body>
<%- include('partials/_header') %>
<p>  <% for (var key in urls) { %>
  <%= key%> ~~~~> <%= urls[key] %>
  <a href="/urls/<%= key%>">Edit</a>
    <form method="POST" action="/urls/<%= key%>/delete">
      <button>Delete</button>
    </form>
  <% } %>
</p>
<a href='/urls/new'>SHORTEN YOUR URL! </a>

</body>
</html>
